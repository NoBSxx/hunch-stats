{
  "name": "hunch-stats",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && npx staticrypt dist/index.html -p %STATS_PASSWORD% --short -d dist -o dist/index.html --remember 30"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.0",
    "chart.js": "^4.4.4",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.0",
    "staticrypt": "^3.5.4",
    "vite": "^6.0.0"
  }
}
